# HANDOFF.md

Current-state snapshot. Updated by whoever last touched the repo. Read this before resuming work.

**Last updated:** 2026-05-19 (Phase 0 nearly complete)

## Where we are

**Phase:** F (first commits per plan §9). Steps 1–8 done. Owner away (dog walk); Phase 0 multi-user RLS test green against live Supabase.

**Done in this session:**
- pnpm monorepo scaffold (`pnpm-workspace.yaml`, root `package.json`, strict TS)
- Next.js 16 in `apps/web` (TypeScript strict, Tailwind v4, App Router, ESLint, src/, `@/*` alias)
- `packages/domain` — `computeRegionFreshness` (DC-C14) + `ewmaStep` (DC-C1), 14/14 Vitest pass
- `packages/db` — Drizzle schema for `profiles`, `limitations` (DC-V1 + DC-A6 region enum + severity enum), `movements` (DC-D4 interference_cost enum). Zod schemas via `drizzle-zod`. Postgres client factory in `client.ts`. `drizzle.config.ts` reads `DATABASE_URL` from `.env.local`
- `packages/engine` and `packages/ui` — stub packages
- **Supabase project provisioned** (https://grhetczkxawkcfgkwerj.supabase.co, region `eu-west-1`)
- **Migrations applied to live Supabase**:
  - `0000_perfect_thunderbird.sql` — generated by Drizzle (tables + enums)
  - `0001_auth_and_rls.sql` — hand-written: FK to `auth.users` with `ON DELETE CASCADE`, `updated_at` triggers, `handle_new_user` trigger (auto-creates profile row on signup), RLS enabled, 11 policies (self-only on profiles + limitations; global-or-self on movements)
- **Supabase Auth wired** in `apps/web` via `@supabase/ssr`:
  - `src/lib/supabase/{client,server,middleware}.ts`
  - `src/middleware.ts` mounted at root, refreshes session every request, redirects on protected routes
  - `src/lib/auth/actions.ts` — `signUp`, `signIn`, `signInWithMagicLink`, `signOut` server actions
  - `src/lib/auth/delete-account.ts` — GDPR account-delete; admin API + cascade
  - `/auth/callback` — OAuth + magic-link callback
  - `/login` — tabbed signin / signup / magic-link form (React useActionState)
  - `/app` — protected page showing the signed-in email + profile row state + sign-out button
  - `/api/health` — DB connectivity check
- **Multi-user RLS verification test** (`packages/db/integration-tests/rls.mjs`): 11/11 pass against live Supabase, covering profiles isolation, limitations RLS, movements global-vs-self, and the full FK cascade on `auth.users` deletion
- `docs/knowledge/` — wiki bootstrap (plan + 3 research files + 108-constraint design-constraints + draft1 + index + log)
- `docs/adr/0001-stack-choices.md` — Phase D verdicts (Supabase, eu-west-1, Supabase Auth, methodology-pure, web-first, free + pricing later)
- `.github/workflows/ci.yml` — pnpm + Node 20, typecheck + lint + unit tests + build, with placeholder NEXT_PUBLIC env vars so the build doesn't fail in CI
- Root docs: `README.md`, `AGENTS.md`, `CHANGELOG.md`, `HANDOFF.md` (this file), strict `tsconfig.base.json`, comprehensive `.gitignore`

## Verified

- `pnpm -r typecheck` ✓ across all 5 workspaces
- `pnpm --filter @hta/domain test` → 14/14 ✓
- `pnpm --filter @hta/web build` ✓ (8 routes including dynamic `/app`, `/auth/callback`, `/login`, `/api/health`)
- `node packages/db/integration-tests/rls.mjs` → 11/11 ✓ live against Supabase
- `node packages/db/verify.mjs` confirms 3 tables RLS=true, 11 policies, 3 triggers, 3 enums

## What's blocked on the owner

| # | Item | Detail |
|---|---|---|
| H3 | **Vercel project** | Connect the GitHub repo (https://github.com/drrowdev/hybrid-training-app) in Vercel; add env vars from `apps/web/.env.local` (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY). Add NEXT_PUBLIC_SITE_URL pointing at the Vercel domain |
| H4 | **Sentry + PostHog DSNs** | Optional for Phase 0; stubbed in `.env.example`. Wire when desired |
| H5 | **Resend Gmail sender** | Deferred per Q5; Supabase Auth's built-in email transport covers signup OTP/magic-link without it |
| H6 | **Google + Apple OAuth** | Plan §4.1 mentions these; not wired in this session. Add via Supabase dashboard → Authentication → Providers, then add `signInWithOAuth` calls to login form |

## Next solo items (after H3 lands)

1. Playwright e2e tests in `apps/web`: critical paths (`/login` → `/app` → sign-out, account-delete)
2. Husky pre-push hook installing `pnpm typecheck && pnpm test && pnpm build`
3. Sentry + PostHog wiring (with consent flow)
4. `.github/workflows/deploy.yml` and `security.yml` (per plan §6.7)
5. Sentry-DSN + PostHog-key gating in `.env.example`
6. Privacy policy + ToS stubs (per plan Phase 0 DoD)
7. Phase 0 → Phase 1 hand-off: movement seed file (~250 entries), `sessions` + `set_logs` + `cardio_logs` + `wellness` tables, the logging UI

## Sensitive files (never commit)

- `apps/web/.env.local` — Supabase URL + publishable key + service-role key + DATABASE_URL (transaction pooler)
- `packages/db/.env.local` — DATABASE_URL (session pooler for migrations)

Both are in `.gitignore`. `apps/web/.env.example` carries safe placeholders.

## Conventions reminder

- Every code commit AI assistants make includes the trailer: `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`
- Every change to wiki pages appends to `docs/knowledge/hybrid-training-log.md`
- Every new wiki page is added to `docs/knowledge/hybrid-training-index.md`
- Phase 0 is "no features visible yet." Out-of-scope work goes to backlog.
